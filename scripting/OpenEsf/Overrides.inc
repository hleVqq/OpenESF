/*
 * Use this forward to override any overridable ("virtual") functions seen in Overridables.inc
 */
public Overrides()
{
    // OVERRIDE(OnClassMenu);
}

// VIRTUAL:OnClassMenu(player) {return PLUGIN_HANDLED;}